
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="myModalLabel">User account activity
      (<a type="button" class="nestedForm" href="<%= admin_edit_customer_path(@user.customer) %>" style="min-width: 0px;">back to edit</a>)
    </h4>
  </div>
  <div class="modal-body">
    <table id="rating_details_table" class="display table table-striped m-b-none" style="font-size: 12px;">
      <thead>
        <tr>
          <th >Date and Time</th>
          <th >Activity</th>
        </tr>
      </thead>
      <tbody>
        <% @activites.each do |activity| %>
          <tr>
            <td><%= activity.created_at.strftime("%A %Y-%m-%d %I:%M %p ") %></td>
            <td><%= activity.activity_type %></td>
          </tr>
        <% end %>
      </tbody>
    </table>    
 
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-md btn-default m-t-sm nestedForm" href="<%= admin_edit_customer_path(@user.customer) %>" style="min-width: 0px;">Back to Edit Customer</button>
    <button type="button" class="btn btn-md btn-default m-t-sm" data-dismiss="modal">Close</button>
    
  </div>